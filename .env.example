# Copy this file to .env and fill in your Anthropic API key.
# Without ANTHROPIC_API_KEY the system runs in deterministic fallback mode —
# the pipeline, validation, retrieval, explanation, and audit trail all work,
# but the LLM decision layer is replaced by rule-based routing.
ANTHROPIC_API_KEY=
